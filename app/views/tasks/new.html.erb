<h1>タスクの新規登録</h1>

<div class="nav justify-content-end">
  <%= link_to '一覧に戻る', tasks_path, class: 'nav-link' %>
</div>

<%= render partial: 'layouts/errors_full_message', locals: { model: @task } %>

<%= render partial: 'form', locals: { task: @task } %>
